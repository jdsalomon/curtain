{
  "name": "curtain",
  "version": "0.3.1",
  "description": "Let your agents show you what they built: bring the stack up, drive the real app, record what happened, tear it down.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "engines": { "node": ">=20.11" },
  "bin": { "curtain": "./bin/curtain" },
  "scripts": {
    "test": "node --test test/unit/*.test.mjs test/integration/*.test.mjs",
    "test:unit": "node --test test/unit/*.test.mjs",
    "test:integration": "node --test test/integration/*.test.mjs"
  }
}
